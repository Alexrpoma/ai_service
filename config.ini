[LLM]
MODEL = meta-llama/Llama-3.1-8B-Instruct
TASK = text-generation
MAX_NEW_TOKENS = 512
PROMPT = Given the following text below, it is a list of items containing summary and URL; for each element, it generates a useful response with the respective URL. each response should not exceed 500 characters.
AUTH_TOKEN = huggingface_token

[SEARCHER]
URL = http://localhost:8000/api/search